# ソフトウェア検証設計

> **位置づけ**：本ドキュメントは ASPICE [SWE.6（Software Verification）](https://en.wikipedia.org/wiki/Automotive_SPICE) に該当する。
> ソフトウェア要件（`20_software_requirements.md`）の各要件に対し、統合されたソフトウェアがそれを満たすことを確認する検証ケースを定義する。
> 各 SWV は、対応するソフトウェア要件（SWR_NNN）へのトレースを持つ。

本書では、承認済みのソフトウェア要件に対する検証ケースを、要件の派生元システム要件ごとに整理して記述する。
検証の実施は段階4-4 で行う。

## 検証手段の分類

各検証ケースは `method` 属性で検証手段を示す。分類は IADT に従う。

| 値 | 手段 | 内容 |
| --- | --- | --- |
| `inspection` | 検査 | 成果物を目視または静的解析で確認する。実行を伴わない |
| `analysis` | 解析 | 計算・モデル・シミュレーションにより間接的に確認する |
| `demonstration` | 実証 | 実行し、期待する動作が観測できることを確認する。定量的な計測を伴わない |
| `test` | 試験 | 実行し、定量的な合否基準に対して計測値を判定する |

`environment` 属性は検証を実施する環境を示す。`host`（開発マシン上）、
`target`（RP2040 実機）、`ci`（GitHub Actions）、`review`（人手による確認）のいずれか。

## 検証ケースの構成

各検証ケースは以下の3項目を持つ。

- **前提条件**：検証を実施できる状態の定義。依存する実装段階と必要な計測手段
- **手順**：実施者が再現できる粒度の操作列
- **合否基準**：観測結果に対する判定条件。可能な限り数値で表現する

1つの検証ケースで複数の要件を確認できる場合は、`verifies` に複数の要件 ID を列挙する。

検証ケースの ID（SWV_NNN）は要件 ID（SWR_NNN）と対応しない。
1つの要件が複数の検証ケースを必要とすることも、1つの検証ケースが複数の要件を確認することもある。
対応関係は `verifies` 属性が正であり、番号の一致は偶然である。

TBD 由来の仮置き値は、検証ケースのタイトルに含めない。値は合否基準に書き、
対応する TBD を `open_params` に記録する。タイトルに含めると、TBD の解決に伴う
値の変更がタイトルの変更を招き、PR やコミットメッセージからの参照が切れる。

前提条件には、対象要件を実装したソフトウェアが動作していることと、観測手段が利用できることを書く。
実装を担当する段階番号や内部コンポーネント名は書かない。
SWE.6 は統合されたソフトウェアが要件を満たすことを確認するプロセスであり、内部構造への言及はコンポーネント検証（SWE.5.2）の領域である。
またアーキテクチャは段階2.5-1 で作り直すため、現時点のコンポーネント名を埋め込むと再設計の結果に連動して修正が必要になる。

本書で初出の略語は、初出箇所で展開形を併記する。

## 検証戦略と網羅基準

### 検証の水準と責任範囲

本書は SWE.6（ソフトウェア検証）に該当し、**ソフトウェア要件が述べていることを基準に**検証を設計する。
同じ技法（同値分割、境界値分析など）は他の水準でも用いるが、何を基準に分割するかが異なる。

| 水準 | 基準となる成果物 | 分割・境界の導出元 |
| --- | --- | --- |
| SWE.6（本書） | ソフトウェア要件 | 要件が明示する値・状態・条件 |
| SWE.5（80 / 70） | アーキテクチャ設計・コンポーネント設計 | インタフェース仕様、コンポーネント間の相互作用
| SWE.4（60 ） | 詳細設計・実装 | 実装が導入した境界（値の桁、配列の印字、条件分岐） |

したがって、本書では以下を扱わない。

- 実装が選択した内部表現に由来する境界（固定小数点の桁落ち、中間変数のオーバーフローなど）。
  要件が型や桁数を明示して場合のみ、その範囲で扱う
- コンポーネント間のインタフェースの網羅
- コードの構造網羅（命令網羅、分割網羅）

### 適用する技法

| 技法 | 適用する場面 | 導出する検証条件 |
| --- | --- | --- |
| 同値分割 | 要件が値の区分や状態の種類を述べている場合 | 各区分の代表値 |
| 境界値分析 | 要件が閾値・上限・下限・桁数を明示している場合 | 境界値そのものと、その両側の値 |
| 状態遷移 | 複数の要件が状態と遷移を規定している場合 | 状態とイベントの全組み合わせ |
| デシジョンテーブル | 複数の条件の組み合わせで結果が決まる場合 | 条件の全組み合わせ |

### 網羅基準

以下を全て満たしたとき、本書の検証設計を完了とみなす。

1. **要件網羅**：承認済みのソフトウェア要件全てが、1 件以上の検証ケースから `verifies` により参照されていること。本基準は `approved_swr_without_verification` により機械的に検証する
2. **境界網羅**：要件が明示する閾値・上限・下限・桁数について、その境界値そのものとその両側の値が検証条件に含まれていること
3. **状態網羅**：状態遷移を持つ要件群について、状態とイベントの全組み合わせが、検証ケースを持つか、発生し得ない旨とその理由が記載されているかのいずれかであること
4. **条件網羅**：複数の条件の組み合わせで結果が決まる要件について、各組み合わせが検証ケースを持つか、発生し得ない旨とその理由が記載されているかのいずれかであること

基準 2〜4 は現時点では機械的に検証できない。状態遷移マトリクスおよびデシジョンテーブルを本書に記載し、空欄がないことをレビューで確認する。
マトリクスの各セルを need として扱い機械的に検証する仕組みは、段階2.5-0-4-3 で導入する。

### 組み合わせの記載方法

状態遷移を持つ要件群については、該当する節に状態とイベントのマトリクスを記載する。
各セルには次のいずれかを記入する。

- 対応する検証するケースの ID
- 「発生し得ない」旨と、その根拠となる要件 ID または物理的な理由

いずれも記入できないセルは、**要件が挙動を規定していない箇所**である。この場合、挙動を推測して検証ケースを作成してはならない。
マトリクスに「要件で未定義」と記載し、段階2.5-1 への申し送りとして記録する。検証設計の目的の一つは、要件の不足を実装前に発見することにある。

## SYS_001（温度の継続的変化可視化）からの派生要件の検証

```{swverif} ADC初期化と温度センサチャネル選択の確認
:id: SWV_001
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_001

**前提条件**

- SWR_001 を実装したソフトウェアが RP2040 実機で動作していること
- UART ログ出力が利用できること

**手順**

1. ADC 初期化後、ADC の制御レジスタと入力選択レジスタの値を UART に出力する
2. 温度センサのバイアス有効化ビットの状態を UART に出力する
3. ADC 変換を1回実行し、取得した生値を UART に出力する

**合否基準**

- 入力チャネルとして ADC チャネル4 が選択されていること
- 温度センサのバイアスが有効であること
- 取得した生値が 0 でも 0xFFF でもないこと（室温付近で飽和していないこと）
```

```{swverif} DMA転送の継続性とラップアラウンドの確認
:id: SWV_002
:status: draft
:method: test
:environment: target
:verifies: SWR_002

**前提条件**

- SWR_002 と SWR_007 を実装したソフトウェアが RP2040 実機で動作していること
- リングバッファの内容と DMA の書き込み位置を UART で観測できること

**手順**

1. リングバッファ全体と DMA の現在の書き込み位置を 1 秒間隔で UART に出力する
2. リングバッファが 3 周する時間以上（バッファ長 × サンプリング周期 × 3 以上）動作させる
3. 各出力における書き込み位置と、バッファ内の値の更新状況を記録する

**合否基準**

- 書き込み位置がバッファ長で折り返し、途切れずに進み続けること
- ラップアラウンド後もバッファの全要素が更新され続けること
- 転送されたデータが 16bit 単位で欠落なく並んでいること（ADC の変換結果として妥当な範囲に収まり、バイト境界のズレがないこと）
```

```{swverif} DMA転送のCPU非介入の確認
:id: SWV_005
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_002

**前提条件**

- SWR_002 を実装したソフトウェアが RP2040 実機で動作していること
- gdb により CPU を停止させた状態で DMA の転送カウンタを読み出せること

**手順**

1. サンプリングを開始し、DMA が転送している状態を確認する
2. gdb でブレークポイントを設定し、CPU を停止させる
3. CPU 停止中に DMA の転送カウンタを 2 回、一定期間を空けて読み出す

**合否基準**

- CPU 停止中も DMA の転送カウンタが進んでいること
- CPU 停止中も ADC FIFO のオーバーフローフラグが立たないこと
```

SWR_002 は「DMA 転送を行うこと」と「CPU 介入なしに継続すること」の 2 つを要求している。
前者は動作を計測すれば確認できるが、後者は動作している事実からは証明できないため、
CPU を停止させた状態で転送が継続することを示す別の検証ケースを設ける。

```{swverif} サンプリング周期の計測
:id: SWV_003
:status: draft
:method: test
:environment: target
:verifies: SWR_003
:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_003 を実装したソフトウェアが RP2040 実機で動作していること
- GPIO のトグル出力をロジックアナライザまたはオシロスコープで観測できること

**手順**

1. ADC サンプリング開始時に GPIO をトグルする計測用コードを有効化する
2. 10 秒間動作させ、トグル間隔を記録する

**合否基準**

- トグル間隔の平均が 10ms ± 0.1ms の範囲内であること
- 連続する 1000 回の計測で 10ms ± 0.5ms を外れる回数が 0 であること
```

```{swverif} I2Cバス初期化と通信確立の確認
:id: SWV_004
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_004
:open_params: TBD-010 I2C速度が未確定のため、合否基準の通信速度は仮置き。解決時期: 段階3-2。TBD-017 GPIO割り当てが未定のため、観測対象のピンは未確定。解決時期: 段階3-2。

**前提条件**

- SWR_004 を実装したソフトウェアが RP2040 実機で動作していること
- I2C の SCL（Serial Clock Line、シリアルクロック線）と SDA（Serial Data Line、シリアルデータ線）をロジックアナライザまたはオシロスコープで観測できること

**手順**

1. I2C 初期化後、LCD1602 のアドレスに対してダミー書き込みを1回行う
2. SCL のクロック周波数を計測する
3. 9 クロック目における ACK（Acknowledge、受信確認応答）の有無を観測する

**合否基準**

- SCL の周波数が 100kHz ± 10% の範囲内であること
- スレーブから ACK が返ること（NACK ではないこと）
- バスがアイドル時に SCL・SDA ともに H レベルに保持されていること（プルアップが効いていること）
```

```{swverif} LCD1602の初期化シーケンスの確認
:id: SWV_006
:status: draft
:method: inspection
:environment: review
:verifies: SWR_005

**前提条件**

- SWR_005 を実装したソースコードが存在すること
- HD44780 のデータシートに記載された初期化シーケンスを参照できること

**手順**

1. LCD の初期化処理のソースコードを読み、送信するコマンド列とその順序を書き出す
2. HD44780 のデータシートが規定する 4 ビットモードの初期化シーケンスと突き合わせる
3. 各コマンド間のディレイの実装値を、データシートが要求する最小値と比較する

**合否基準**

- コマンド列と順序がデータシートの初期化シーケンスと一致すること
- 各ディレイがデータシートの要求する最小値以上であること
- PCF8574（I2C 経由の 8 ビット I/O エクスパンダ）への書き込みが上位 4 ビットと下位 4 ビットに分割されていること
```

```{swverif} LCD1602への表示成立の確認
:id: SWV_007
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_005

**前提条件**

- SWR_004 と SWR_005 を実装したソフトウェアが RP2040 実機で動作していること
- LCD1602 が接続され、目視できること

**手順**

1. 初期化後、既知の文字列を 1 行目と 2 行目に書き込む
2. 表示内容を目視で確認する

**合否基準**

- 意図した文字列が意図した行・桁位置に表示されること
- 文字化け、欠落、ゴースト表示がないこと
```

SWR_005 の初期化シーケンスの妥当性は、表示ができることでは保証されない。
ディレイが不足していても室温では動作してしまい、個体差や温度条件で失敗する不具合になるため、
データシートと突き合わせを検査として分離する。一方で配線やアドレス指定の誤りは検査では見つからないため、実機での表示検証も行う。

```{swverif} 表示更新周期の計測
:id: SWV_008
:status: draft
:method: test
:environment: target
:verifies: SWR_006
:open_params: TBD-001 表示周期500msは仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_006 を実装したソフトウェアが RP2040 実機で動作していること
- LCD への書き込み開始時点を GPIO のトグルまたは I2C バスの通信開始で観測できること

**手順**

1. LCD 表示更新の開始時点を観測できる状態にする
2. 60 秒間動作させ、更新間隔を全て記録する

**合否基準**

- 更新間隔の平均が 500ms ± 10ms の範囲内であること
- 記録した全区間において、更新間隔が 500ms を下回らないこと
```

SWR_006 は「500ms 周期で更新する」ことに加えて「500ms 未満の周期で更新しない」ことを要求している。
平均値だけでは後者を確認できないため、全区間の最小を合否基準に含める。

## SYS_002（統計値の算出と表示）からの派生要件の検証

```{swverif} リングバッファのサイズと要素型の確認
:id: SWV_009
:status: draft
:method: analysis
:environment: review
:verifies: SWR_007
:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、必要サンプル数が連動して変わる。解決時期: 段階4-4。:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、必要サンプル数が連動して変わる。解決時期: 段階4-4。TBD-004 トレンド判定対象期間1分は仮置きのため、必要サンプル数が連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_007 を実装したソースコードが存在すること
- SYS_003 が規定するトレンド判定対象期間が確定していること

**手順**

1. トレンド判定対象期間とサンプリング周期から、保持すべき最小サンプル数を算出する
2. 算出値以上となる最小の 2 の冪乗を求める
3. 実装されたバッファサイズおよび要素型の定義と突き合わせる

**合否基準**

- バッファサイズが 2 の冪乗であること
- バッファサイズが手順 2 で求めた値と一致すること
- 要素が 16bit unsigned integer であること
```

```{swverif} リングバッファのハードウェア非依存性の確認
:id: SWV_010
:status: draft
:method: demonstration
:environment: host
:verifies: SWR_007

**前提条件**

- SWR_007 を実装したソースコードが存在すること
- 開発マシン上の C コンパイラが利用できること

**手順**

1. リングバッファのソースファイルを、RP2040 向けではなく開発マシン向けにビルドする
2. ビルドしたオブジェクトをホスト上で実行し、要素の書き込みと読み出しを行う

**合否基準**

- RP2040 固有のヘッダやレジスタ定義を参照せずビルドが完了すること
- ホスト上で書き込みと読み出しが期待通り動作すること
```

SWR_007 は「データ構造の仕様」と「ハードウェア非依存であること」を要求している。
前者は実装を読めば確認できるが、後者はホスト環境でビルドおよび実行が成立することを示す方が確実である。
レジスタアクセスの有無をコードの目視だけで保証するのは、間接的な include を見落とす危険がある。

```{swverif} スナップショット取得の順序と範囲の確認
:id: SWV_011
:status: draft
:method: test
:environment: host
:verifies: SWR_008

**前提条件**

- SWR_007 と SWR_008 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. リングバッファに既知の連番データを書き込む
2. 書き込み位置がバッファの途中にある状態で、指定サンプル数のスナップショットを取得する
3. 書き込み位置がラップアラウンドを跨いだ状態で、同様にスナップショットを取得する
4. 指定サンプル数をバッファ長と等しくした場合、および 1 とした場合をそれぞれ取得する

**合否基準**

- 取得したサンプル列が、書き込み位置から遡って古い順に並んでいること
- ラップアラウンドを跨ぐ場合も、途切れや重複なく連続していること
- 指定サンプル数と取得件数が一致すること
```

SWR_008 が許容する「最大1サンプル分の不整合」は DMA による並行書き込みが前提であり、ホスト環境では再現しない。
本検証はスナップショットの順序と範囲の正しさに限定し、並行書き込み下での挙動は実機での確認に委ねる。

```{swverif} 統計値計算の正しさの確認
:id: SWV_012
:status: draft
:method: test
:environment: host
:verifies: SWR_009

**前提条件**

- SWR_009 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 最大値・最小値・平均値が既知であるサンプル列を入力し、計算結果を取得する
2. 全要素が同一値のサンプル列を入力する
3. 16bit unsigned integer の最大値（65535）を含むサンプル列を入力する
4. 入力サンプル数を変えて、それぞれ計算結果を取得する

**合否基準**

- 最大値・最小値が入力の実際の最大・最小と一致すること
- 平均値が期待値と一致すること（丸め方は実装の定義に従い、期待値もそれに合わせる）
- 全要素同一の場合、最大値・最小値・平均値が全てその値と一致すること
- 指定したサンプル数が計算に反映されること
- 総和が 16bit の範囲を超える入力でも平均値が正しく求まること
```

```{swverif} 「最新値」の算出範囲の確認
:id: SWV_013
:status: draft
:method: test
:environment: host
:verifies: SWR_010
:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、直近1秒に相当するサンプル数が連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_009 と SWR_010 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 直近 1 秒分とそれ以前で異なる値を持つサンプル列をリングバッファに書き込む
2. 「最新値」を取得する

**合否基準**

- 直近 1 秒分に相当するサンプル数のみが平均の対象になっていること
- 1 秒より前のサンプルが結果に影響しないこと
```

```{swverif} 温度変換の正しさと分解能の確認
:id: SWV_014
:status: draft
:method: test
:environment: host
:verifies: SWR_011

**前提条件**

- SWR_011 を実装したソフトウェアがホスト上でビルドできること
- RP2040 のデータシートに記載された温度変換式を参照できること

**手順**

1. データシートの変換式から、代表的な ADC 生値に対する期待温度を手計算で求める
2. 同じ ADC 生値を入力し、変換結果と期待値を比較する
3. 隣接する ADC 生値（N と N+1）を入力し、出力の差を確認する
4. 較正係数を変更し、同じ ADC 生値に対する出力の変化を確認する

**合否基準**

- 変換結果がデータシートの式から求めた期待値と一致すること（固定小数点実装の場合、丸め誤差は分解能の範囲内に収まること）
- 出力の分解能が 0.1℃ 以上であること
- 較正係数の変更が出力に反映されること
```

内部表現（浮動小数点か固定小数点か）は SWR_011 が規定しておらず段階2.5-3 で決定する。
本検証は入出力の関係のみを対象とし、内部表現には依存しない形で合否基準を定める。

```{swverif} 表示フォーマットの桁数と内容の確認
:id: SWV_015
:status: draft
:method: test
:environment: host
:verifies: SWR_012
:open_params: TBD-011 表示レイアウトの最終形が未確定のため、配置に関する合否基準は暫定。解決時期: 段階4-4。

**前提条件**

- SWR_012 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 統計値（最新値・最大値・最小値・平均値）に代表的な値を与え、生成される文字列を取得する
2. 温度が負値の場合、および 3 桁になる場合の文字列を取得する
3. 各行の文字数を数える

**合否基準**

- 各行が 16 文字以内であること
- 行数が 2 行以内であること
- 最新値・最大値・最小値・平均値の 4 値が全て含まれること
- 温度が小数点以下 1 桁まで表示されること
- 負値および 3 桁の値でも桁あふれしないこと
```

## SYS_003（温度変化のトレンド表示）からの派生要件の検証

```{swverif} トレンド判定モジュールの提供機能の確認
:id: SWV_016
:status: draft
:method: test
:environment: host
:verifies: SWR_013

**前提条件**

- SWR_013 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 上昇・下降・横ばいのそれぞれに該当するサンプル列を用意する
2. 各サンプル列を入力し、判定結果を取得する

**合否基準**

- 3 つの傾向がいずれも判定結果として得られること
- 同一の入力に対して常に同一の結果が返ること
```

```{swverif} トレンド判定のモジュールのハードウェア被依存性の確認
:id: SWV_017
:status: draft
:method: demonstration
:environment: host
:verifies: SWR_013

**前提条件**

- SWR_013 を実装したソースコードが存在すること
- 開発マシン上の C コンパイラが利用できること

**手順**

1. トレンド判定モジュールを、統計モジュールともに開発マシン向けにビルドする
2. ビルドしたオブジェクトをホスト上で実行し、判定を行う

**合否基準**

- RP2040 固有のヘッダやレジスタ定義を参照せずにビルドが完了すること
- ホスト上で判定が期待通り動作すること
- 統計モジュール以外のモジュールへの依存が生じていないこと
```

```{swverif} 前半後半平均比較アルゴリズムの確認
:id: SWV_018
:status: draft
:method: test
:environment: host
:verifies: SWR_014
:open_params: TBD-004 トレンド判定対象期間1分は仮置きのため、分割されるサンプル数が連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_009 と SWR_014 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 前半と後半で平均が既知の差を持つサンプル列を用意し、判定に用いられた差分値を取得する
2. 判定対象期間と同数のサンプルを与え、前半・後半それぞれの平均を手計算の値と比較する
3. 判定対象期間の中央付近にのみ大きな変化があるサンプル列を与え、差分値を確認する

**合否基準**

- 対象期間が前半と後半に二等分されていること
- 前半・後半の平均が手計算の値と一致すること
- 差分が「後半平均 - 前半平均」の順で計算されていること（温度が上昇するサンプル列に対して差分が正になること）
- 中央付近の一時的な変化が、前半・後半の平均に希釈されて差分に強く現れないこと
```

手順 3 は、始点と終点の単純比較ではなく平均の比較を採用した理由
（SWR_014 の rationale が挙げるノイズへのロバスト性）が実際に成立していることの確認である。
実装が単純比較になっていた場合、手順 1・2 だけでは差が現れないことがある。

```{swverif} トレンド判定の閾値と境界の確認
:id: SWV_019
:status: draft
:method: test
:environment: host
:verifies: SWR_015
:open_params: TBD-012 閾値0.3℃は仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_014 と SWR_015 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 差分が閾値をわずかに超える正の値となるサンプル列を入力する
2. 差分が閾値とちょうど等しい正の値となるサンプル列を入力する
3. 差分が閾値をわずかに下回る正の値となるサンプル列を入力する
4. 手順 1〜3 と同じ大きさの負の差分となるサンプル列をそれぞれ入力する

**合否基準**

- 差分が閾値を超え正の値のとき、上昇傾向と判定されること
- 差分が閾値を超え負の値のとき、下降傾向と判定されること
- 差分の絶対値が閾値と等しいとき、横ばいと判定されること
- 差分の絶対値が閾値未達のとき、横ばいと判定されること
```

SWR_015 は「0.3℃を超え」「0.3℃以下であれば横ばい」と境界の扱いを明記している。
境界値そのものを与える手順 2 を含めない限り、不等号の向きの誤り（`>` と `>=` の取り違え）は検出できない。

```{swverif} トレンド判定の実行周期と表示との同期の確認
:id: SWV_020
:status: draft
:method: test
:environment: target
:verifies: SWR_016
:open_params: TBD-001 実行周期500msは仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_016 を実装したソフトウェアが RP2040 実機で動作していること
- トレンド判定の開始時点と LCD 表示更新の開始時点を、それぞれ GPIO のトグルで観測できること

**手順**

1. トレンド判定の開始時点と表示更新の開始時点を、別々の GPIO で観測できる状態にする
2. 60 秒間動作させ、両者の発生時刻を全て記録する

**合否基準**

- トレンド判定の実行間隔の平均が 500ms ± 10ms の範囲内であること
- トレンド判定の回数と表示更新の回数が一致すること
- 各表示更新の直前にトレンド判定が完了していること
```

SWR_016 は実行周期に加えて「結果を表示に反映すること」を要求している。
周期の計測だけでは、判定結果が表示に届く前に上書きされる実装を検出できないため、両者の順序と回数の一致を合否基準に含める。

```{swverif} トレンド記号の表示の確認
:id: SWV_021
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_017
:open_params: TBD-013 表示記号の最終形が未確定のため、合否基準の記号は暫定。解決時期: 4-4。

**前提条件**

- SWR_012 と SWR_017 を実装したソフトウェアが RP2040 実機で動作していること
- LCD1602 が接続され、目視できること
- 温度を意図的に上昇・下降させる手段があること（センサ部への加温・冷却）

**手順**

1. 温度を上昇させ、判定が上昇傾向に変わった時点の表示を確認する
2. 温度を下降させ、判定が下降傾向に変わった時点の表示を確認する
3. 温度を安定させ、横ばいと判定される状態の表示を確認する

**合否基準**

- 上昇・下降・横ばいの 3 状態が、それぞれ異なる記号で表示されること
- 記号が SWR_012 の表示フォーマット中に収まり、他の表示項目を欠落させないこと
- 記号が判定結果と一致すること
```

記号の具体的な字形は「TBD-013」により未確定であるため、合否基準では「3. 状態が区別できること」と「フォーマットに収まること」の限定する。
字形の決定は段階4-4 で実機の視認性を確認した後に行う。

## SYS_004(デュアルコアによる並行処理)からの派生要件の検証

```{swverif} Core1起動機構の確認
:id: SWV_022
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_020

**前提条件**

- SWR_020 を実装したソフトウェアが RP2040 実機で動作していること
- 実行中のコア ID を UART ログに出力できること

**手順**

1. Core1 のエントリ関数として、コア ID とスタックポインタの値を UART に出力する関数を指定する
2. Core1 用のスタック領域を確保し、そのポインタとエントリ関数を引数に起動関数を呼び出す
3. Core1 からの出力を確認する

**合否基準**

- Core1 が起動し、指定したエントリ関数が実行されること
- Core1 が報告するコア ID が 1 であること
- Core1 のスタックポインタが、引数として渡した領域の範囲内にあること
```

```{swverif} Core1起動タイミングの確認
:id: SWV_023
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_021

**前提条件**

- SWR_021 を実装したソフトウェアが RP2040 実機で動作していること
- 各初期化処理の完了時点と Core1 の起動時点と UART ログで観測できること

**手順**

1. ADC・DMA・タイマーの各初期化完了時点と、Core1 起動関数の呼び出し時点、Core1 のエントリ関数の実行開始時点をそれぞれ UART に出力する
2. リセット直後からのログを記録する

**合否基準**

- ADC・DMA・タイマーの初期化完了が全て Core1 起動関数の呼び出しより先に出力されること
- Core1 のエントリ関数の実行開始が、起動関数の呼び出しより後に出力されること
```

SWR_021 は「サンプリング機構の初期化完了後に起動すること」という順序を要求している。
Core1 が起動する事実だけでは順序を確認できないため、初期化と起動の各時点を時系列で記録し、前後関係を合否基準とする。

```{swverif} アイドル時のCPU非消費の確認
:id: SWV_024
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_024

**前提条件**

- SWR_024 を実装したソフトウェアが RP2040 実機で動作していること
- gdb により両コアの実行を停止させ、プログラムカウンタを読み出せること

**手順**

1. 定常動作状態（イベント処理の合間）で、Core0 と Core1 の実行をそれぞれ停止させる
2. 停止した時点のプログラムカウンタが指す命令を確認する
3. 手順 1〜2 を、時間を空けて 10 回繰り返す

**合否基準**

- 停止時のプログラムカウンタが、待機命令またはその直後を指す回数が大半を占めること
- ループ内で条件を繰り返し評価している箇所で停止する回数が 0 であること
```

```{swverif} busy-waitの不在の確認
:id: SWV_025
:status: draft
:method: inspection
:environment: review
:verifies: SWR_024

**前提条件**

- SWR_024 を実装したソースコードが存在すること

**手順**

1. Core0 と Core1 のメインループのソースコードを読む
2. ループ内の待機箇所を全て列挙する
3. 各待機箇所が、待機命令によるものかフラグの繰り返し評価によるものかを分類する

**合否基準**

- メインループ内の待機が全て待機命令によるものであること
- フラグやレジスタの値を繰り返し読んで条件成立を待つ箇所が存在しないこと
```

SWR_024 は「CPU を消費しないこと」「busy-wait を行わないこと」という否定形の要求である。
実行時の観測は確率的な標本にとどまり、busy-wait 区間が短ければ検出できない。
不在を示すには実装の検査が必要であり、一方で検査だけでは実際に待機命令に到達していることを確認できないため、両方を行う。

## SYS_005（コア間データ受け渡しの整合性）からの派生要件の検証

```{swverif} 計算結果の中間状態非露出の確認
:id: SWV_026
:status: draft
:method: test
:environment: target
:verifies: SWR_026

**前提条件**

- SWR_026 と SWR_028 を実装したソフトウェアが RP2040 実機で動作していること
- Core0 が受信した構造体の全フィールドを UART に出力できること

**手順**

1. Core1 が送信する構造体の全フィールドに、同一世代であることが判定できる値を設定する（例：各フィールドに世代番号を含める、または相互に矛盾しない値の組を用いる）
2. Core0 で受信するたびに、構造体の全フィールドを UART に出力する
3. 送信周期の 200 倍以上の回数を受信するまで動作させる

**合否基準**

- 受信したすべての構造体において、フィールドの組が同一世代のものであること
- min <= average <= max の関係が全受信結果で成立すること
- 新旧の世代が混在した構造体が 1 件も観測されないこと
```

SWR_026 は「中間状態を読み出さないこと」という否定形の保証を要求している。
単発の受信では中間状態が発生しないことも多いため、多数回の受信を通じて混在が 1 件現れないことを合否基準とする。
混在が原理的に起こり得ないことの保証は SWR_027 の機構（SIO FIFO のハードウェア原子性）と SWR_029 の受信側実装に依存しており、それぞれ SWV_027 と SWV_029 で確認する。

```{swverif} SIO FIFO送受信関数の確認
:id: SWV_027
:status: draft
:method: test
:environment: target
:verifies: SWR_027

**前提条件**

- SWR_027 を実装したソフトウェアが RP2040 実機で動作していること
- 両コアから UART ログを出力できること
- gdb により各コアの SIO の FIFO 状態レジスタを読み出せること
- 試験中に Core0 が受信を行わない状態を作れること（送信 FIFO を満杯にするため）

**手順**

1. Core1 から既知の値を持つ 32bit ワードを送信し、Core0 で受信して値を UART に出力する
2. 受信 FIFO が空の状態で Core0 のノンブロッキング受信関数を呼び出し、戻り値を UART に出力する
3. 受信 FIFO が空の状態で Core0 のブロッキング受信関数を呼び出す。gdb で Core0 が復帰していないことを確認した後、Core1 から送信して復帰することを確認する
4. Core0 が受信を行わない状態で、Core1 から FIFO の段数を超える回数を連続送信し、満杯チェック関数の戻り値を記録する
5. 手順 1〜4 の各時点で、gdb により両コアの FIFO 状態レジスタを読み出す

**合否基準**

- 送信した値と受信した値が一致すること
- 受信 FIFO が空のとき、ノンブロッキング受信が待機せずデータ無しを示す結果を返すこと
- 受信 FIFO が空のとき、ブロッキング受信が送信されるまで復帰しないこと
- 満杯チェック関数の戻り値が、状態レジスタの送信可能ビットと一致すること
- 空チェック関数の戻り値が、状態レジスタの受信有効ビットと一致すること
- 一連の試験を通じて、空読み出しおよび満杯書き込みのスティッキーエラーフラグが立たないこと
```

SIO FIFO は読み出しにより要素が取り出されるため、内容を覗いてから API を呼ぶことはできない。
観測は状態レジスタのフラグと API の戻り値によって行う。
スティッキーエラーフラグは一度立つとクリアするまで保持されるため、試験期間中に一度でも誤ったアクセスが起きたことを取りこぼさずに検出できる。

```{swverif} 構造体受信APIの対称性の確認
:id: SWV_028
:status: draft
:method: test
:environment: target
:verifies: SWR_028

**前提条件**

- SWR_027 と SWR_028 を実装したソフトウェアが RP2040 実機で動作していること

**手順**

1. 全フィールドの異なる既知の値を設定した構造体を Core1 から送信する
2. Core0 で受信した構造体の各フィールドを、送信時の値と比較する
3. 各フィールドが取り得る境界値（0、型の最大値、負値を取るフィールドがあればその最小値）を設定した構造体でも同様に送受信する

**合否基準**

- 受信した構造体の全フィールドが、送信した値と一致すること
- フィールドの順序が入れ替わっていないこと
- 境界値でも値が変化しないこと（符号拡張やパディングによる破損がないこと）
```

構造体を複数ワードに分解して送るため、分解と組み立ての順序が送受信で食い違っていても、全フィールドが同じ値であれば検出できない。
全フィールドに異なる値を設定するのは、順序の食い違いを検出するためである。

```{swverif} 部分受信時の非返却の確認
:id: SWV_029
:status: draft
:method: test
:environment: target
:verifies: SWR_029

**前提条件**

- SWR_028 と SWR_029 を実装したソフトウェアが RP2040 実機で動作していること
- Core1 側で構造体の一部のワードのみを送信できる試験用の経路があること

**手順**

1. Core1 から構造体を構成するワードのうち、先頭の一部のみを送信する
2. Core0 の受信 API を呼び出し、戻り値と出力を確認する
3. 残りのワードを送信し、再度受信 API を呼び出す
4. 手順 1〜3 を、途中で打ち切るワード数を変えて繰り返す

**合否基準**

- 全ワードが揃う前は、受信 API が構造体を返さないこと
- 全ワードが揃った時点で、送信した値と一致する構造体が返ること
- 打ち切り位置を変えても、部分的な構造体が返らないこと
```

本検証には、構造体の一部のみを送信する試験用の経路が必要である。
この経路を製品コードにどう設けるか（条件コンパイル、試験用関数の分離など）は段階2.5-2 のコンポーネント設計で決定する。

## SYS_006（I2C通信障害の検出と通知）空の派生要件の検証

```{swverif} I2C送信失敗時の戻り値の確認
:id: SWV_030
:status: draft
:method: test
:environment: target
:verifies: SWR_031

**前提条件**

- SWR_031 を実装したソフトウェアが RP2040 実機で動作していること
- I2C バス上のスレーブを意図的に応答不能にできること（電源の切断、または未使用アドレスの指定）
- 送信関数の戻り値を UART に出力できること

**手順**

1. 正常な状態で送信関数を呼び出し、戻り値を記録する
2. 未使用の I2C アドレスに対して送信関数を呼び出し、戻り値を記録する
3. SDA を Low に固定してバスを占有した状態で送信関数を呼び出し、戻り値と関数から復帰するまでの時間を記録する

**合否基準**

- 正常時に成功を示す値が変えること
- NACK 発生時に失敗を示す値が返ること
- バス占有時に失敗を示す値が返り、有限時間で復帰すること
- NACK とタイムアウトが戻り値から区別できること
```

```{swverif} I2C HALのステートレス性の確認
:id: SWV_031
:status: draft
:method: inspection
:environment: review
:verifies: SWR_031

**前提条件**

- SWR_031 を実装したソースコードが存在すること

**手順**

1. I2C HAL のソースコードを読み、宣言されている変数を全て列挙する
2. 各変数を、関数のローカル変数と、関数を跨いで保持される変数に分類する
3. 関数を跨いで保持される変数について、その用途を確認する

**合否基準**

- 失敗回数を保持する変数が存在しないこと
- 通信の成否に関する状態を関数呼び出しを跨いで保持していないこと
```

SWR_031 は「状態を保持しないこと」という否定形の要求を含む。
実行時の観測では状態の不在を示せないため、実装の検査により確認する。

```{swverif} エラー状態への遷移と失敗の即時応答の確認
:id: SWV_032
:status: draft
:method: test
:environment: target
:verifies: SWR_033
:open_params: TBD-007 連続失敗の閾値5回は仮置きのため、合否基準の回数は連動して変わる。解決時期: 段階4-3。

**前提条件**

- SWR_033 を実装したソフトウェアが RP2040 実機で動作していること
- LCD を応答不能にできること（電源の切断）
- 表示関数の戻り値と、関数の所要時間を UART に出力できること

**手順**

1. 正常動作中に LCD の電源を切断する
2. 表示要求ごとの戻り値と所要時間を、電源切断から 20 回分以上記録する
3. I2C バス上の通信の有無をロジックアナライザで観測する
4. エラー状態のまま LCD の電源を再投入し、次の回復試行が行われる前に表示要求を発行して、戻り値と所要時間を記録する

**合否基準**

- 電源切断後の 1 回目から 5 回目までの表示要求では、I2C 送信が試行されて失敗が返ること
- 6 回目以降の表示要求では I2C 送信が行われず、送信の完了を待たずに失敗が返ること（所要時間が 1〜5 回目に比べて有意に短いこと）
- エラー状態に入った後も、バックグラウンドで I2C 送信の試行が継続していること
- LCD が応答可能になっていても、回復試行が成功するまでは表示要求に対して即座に失敗が返ること
```

SWR_033 は「即座に失敗を返す」ことと「バックグラウンドで試行を継続する」ことを同時に要求している。
戻り値だけでは両者を区別できないため、表示関数の所要時間とバス上の通信の有無を観測対象とする。
手順 4 は、回復試行の契機を表示要求と独立させる設計を前提とする。表示要求に便乗する設計を採った場合の挙動は TBD-021 として申し送る。

```{swverif} 表示関数のエラー戻り値の確認
:id: SWV_033
:status: draft
:method: test
:environment: target
:verifies: SWR_034

**前提条件**

- SWR_034 を実装したソフトウェアが RP2040 実機で動作していること
- LCD を応答不能にできること
- 表示関数の戻り値を UART に出力できること

**手順**

1. 正常時に表示関数を呼び出し、戻り値を記録する
2. LCD の電源を切断し、エラー状態に入る前（連続失敗の途中）の戻り値を記録する
3. エラー状態に入った後の戻り値を記録する

**手順**

- 正常時に成功を示す値が返ること
- 送信失敗時に失敗を示す値が返ること
- エラー状態のときに失敗を示す値が返ること
```

```{swverif} 回復検出と再初期化の確認
:id: SWV_034
:status: draft
:method: test
:environment: target
:verifies: SWR_035

**前提条件**

- SWR_035 を実装したソフトウェアが RP2040 実機で動作していること
- LCD の電源を任意のタイミングで切断・投入できること
- I2C バス上の通信をロジックアナライザで観測できること

**手順**

1. LCD の電源を切断し、エラー状態に遷移させる
2. エラー状態のまま 10 秒以上放置し、I2C 送信の試行間隔を記録する
3. LCD の電源を再投入する
4. 電源再投入から通常表示が再開されるまでのバス上の通信を記録する
5. 手順 1〜4 を 3 回繰り返す

**合否基準**

- エラー状態の間、定期的に I2C 送信が試行されていること
- 電源再投入後、送信成功を契機として HD44780 の初期化シーケンスが検出されること
- 初期化シーケンスの後に通常の表示データが送出されること
- 3 回とも表示が正常に再開されること（文字化けや欠落がないこと）
```

再初期化が実行されたことは、バス上に初期化シーケンス特有のコマンド列が現れることで判別する。
表示が再開された事実だけでは、初期化を経ずに表示データを送っている実装と区別できない。
電源断後の LCD は初期化なしでは正しく表示できないため、多くの場合は文字化けとして現れるが、常にそうなるとは限らない。

```{swverif} 際初期化中のビジー応答の確認
:id: SWV_035
:status: draft
:method: test
:environment: target
:verifies: SWR_036

**前提条件**

- SWR_036 を実装したソフトウェアが RP2040 実機で動作していること
- 再初期化シーケンスの実行中に表示要求を発行できること
- 表示関数の戻り値を UART に出力できること

**手順**

1. LCD の電源を切断してエラー状態に遷移させた後、電源を再投入する
2. 再初期化シーケンスの実行中に表示要求を発行し、戻り値を記録する
3. 再初期化の完了後に表示要求を発行し、戻り値を記録する
4. 手順 2 で発行した表示要求の内容が、LCD に表示されていないことを確認する

**合否基準**

- 再初期化中の表示要求に対して、ビジーを示す戻り値が返ること
- ビジーを示す戻り値が、通常の失敗を示す戻り値と区別できること
- 再初期化の完了後に表示要求を発行し、戻り値と区別できること
- 再初期化中に発行した表示要求の内容が、後から遅れて表示されないこと
```

再初期化シーケンスは数 ms 程度であり、表示周期 500ms との比率から、通常動作ではこのタイミングに表示要求が重なることは稀である。
検証には意図的にタイミングを合わせる手段必要であり、その実現方法は段階2.5-2 のコンポーネント設計で決定する。

**LCD Driver の状態遷移マトリクス**

SWR_033〜036 は LCD Driver の状態と遷移を規定している。状態とそのイベントの全組み合わせを以下に示す。
SWR_033 の閾値の境界を網羅するため、連続失敗数が閾値の直前（4 回）である状態と、それ未満（0〜3 回）である状態を分けて扱う。

イベントは以下の通り定義する。

- **表示要求(応答可)**：Application 層からの表示要求。LCD が I2C 送信に応答できる状況
- **表示要求(応答不可)**：同上。LCD が I2C 送信に応答できない状況
- **内部送信の成功**：LCD Driver が自ら行う I2C 送信の成功。エラー状態では回復試行を、再初期化中では初期化シーケンスの送信を指す
- **内部送信の失敗**：同上の送信の失敗

| 状態 | 表示要求（応答可） | 表示要求（応答不可） | 内部送信の成功 | 内部送信の失敗 |
| --- | --- | --- | --- | --- |
| 正常（連続失敗 0〜3 回） | 成功を返す（SWV_033）。連続失敗数の扱いは未定義（TBD-019） | 失敗を返し、連続失敗数を加算する（SWV_032、SWV_033） | 発生し得ない（SWR_035 により内部送信はエラー状態を再初期化中に限られる） | 発生し得ない（同左） |
| 正常（連続失敗 4 回） | 成功を返す（SWV_033）。連続失敗の扱いは未定義（TBD-019） | 失敗を返し、エラー状態へ遷移する（SWV_034） | エラー状態を維持する |
| エラー | 即座に失敗を返す（SWV_032）。TBD-021 に依存 | 即座に失敗を返す（SWV_032） | TBD-021 に依存 | 再初期化中へ遷移する（SWV_034） | エラー状態を維持する（SWV_034） |
| 再初期化中 | ビジーを返す（SWV_035） | ビジーを返す（SWV_035）。表示要求は送信を伴わないため、LCD の応答可否に依らない | 初期化シーケンスの完了をもって正常へ遷移する（SWV_034）。遷移後の連続失敗数は未定義（TBD-019） | **要件で未定義（TBD-020）** |

要件が挙動を規定していない事項は以下のとおりである。いずれも挙動を推測して検証ケースを作成せず、構造的 TBD として該当要件の `tbd_items` に記録した。
ADR-0005 の再承認の運用に従い、該当要件は draft に差し戻している。

- **TBD-019**（SWR_033）：連続失敗数のリセット条件。表示要求が成功したときにリセットするのか、再初期化が完了したときにリセットするのかが規定されていない。このため「4 回失敗した後に 1 回成功し、再び失敗した場合」の挙動が定まらず、閾値の境界の片側を検証できない。欠番である SWR_032 との関係も含めて確認が必要である
- **TBD-020**（SWR_035）：再初期化シーケンスの実行中に I2C 送信が失敗した場合の遷移先。エラー状態へ戻るのか、シーケンスを最初からやり直すのかが規定されていない。LCD の電源が不安定な状況で現実に発生し得る
- **TBD-021**（SWR_035）：SWR_035 の rationale は、回復試行の契機として表示要求に便乗する計画を許容している。一方で SWR_033 は、エラー状態の表示要求に対して即座に失敗を返すことを求めている。表示要求に便乗して I2C 送信を行うと「即座に」を満たさないため、両要件の整合を確認する必要がある

```{swverif} GPIO HALの操作と状態非保持の確認
:id: SWV_036
:status: draft
:method: test
:environment: target
:verifies: SWR_037

**前提条件**

- SWR_037 を実装したソフトウェアが RP2040 実機で動作していること
- 任意の GPIO ピンの出力をテスタまたはロジックアナライザで観測できること

**手順**

1. 未使用の GPIO ピンを出力モードに設定し、ピンの状態を観測する
2. 同じピンを High、Low の順に設定し、それぞれ出力を観測する
3. 異なる 2 本のピンに対して交互に操作を行い、互いに影響しないことを確認する
4. GPIO HAL のソースコードを読み、関数を跨いで保持される変数がないことを確認する

**合否基準**

- 指定したピンが出力モードに設定すること
- High / Low の指定が出力に反映されること
- 一方のピンへの操作が他方のピンの出力を変化させないこと
- ピンの状態や設定履歴を保持する変数が存在しないこと
```

```{swverif} LED点滅パターン管理の確認
:id: SWV_037
:status: draft
:method: test
:environment: target
:verifies: SWR_038
:open_params: TBD-008 点滅パターン（周期、デューティー比）が未決定のため、合否基準のパターンは暫定。解決時期: 段階4-4。

**前提条件**

- SWR_038 を実装したソフトウェアが RP2040 実機で動作していること
- オンボード LED の状態を目視またはロジックアナライザで観測できること

**手順**

1. 点滅を有効化し、LED の変化を 10 秒間観測する
2. 点滅を無効化し、LED の状態を観測する
3. 有効化と無効化を 5 回繰り返す
4. LED Driver のソースコードを読み、GPIO HAL に渡している引数を確認する

**合否基準**

- 有効化により LED が周期的に点滅すること
- 無効化により点滅が停止すること
- 有効化・無効化の繰り返しで動作が変化しないこと
- ピン番号が LED Driver 内の定数として定義され、GPIO HAL には引数として渡されていること（GPIO HAL が特定のピンを前提としていないこと）
```

```{swverif} エラー検出時のLED点滅開始の確認
:id: SWV_038
:status: draft
:method: test
:environment: target
:verifies: SWR_039
:open_params: TBD-007 連続失敗の閾値5回は仮置きのため、点滅開始までの回数が連動して変わる。解決時期: 段階4-3。

**前提条件**

- SWR_039 を実装したソフトウェアが RP2040 実機で動作していること
- LCD を応答不能にできること
- LED の状態と表示関数の戻り値を、同一の時間軸で観測できること

**手順**

1. 正常動作中の LED の状態を確認する
2. LCD の電源を切断し、LED が点滅を開始するまでの表示要求の回数を記録する
3. LCD の電源を再投入し、LED が点滅を停止するまでの表示成功の回数を記録する

**合否基準**

- 正常動作中は LED が点滅しないこと
- 表示関数が失敗を返し続けた後に LED が点滅を開始すること
- 表示が成功するようになった後に LED が点滅を停止すること
```

```{swverif} LED通知停止の連続成功判定の確認
:id: SWV_039
:status: draft
:method: test
:environment: target
:verifies: SWR_040
:open_params: TBD-018 連続成功の閾値3回は仮置きのため、合否基準の回数は連動して変わる。解決時期: 段階4-3。

**前提条件**

- SWR_040 を実装したソフトウェアが RP2040 実機で動作していること
- LCD の応答を任意のタイミングで成功・失敗させられること
- LED の状態と表示関数の戻り値を、同一の時間軸で観測できること

**手順**

1. LED が点滅している状態を作る
2. 表示を 1 回だけ成功させ、その直後に再び失敗させる。LED の状態を観測する
3. 表示を 2 回連続で成功させ、その直後に失敗させる。LED の状態を観測する
4. 表示を 3 回連続で成功させ、LED の状態を観測する

**合否基準**

- 成功が 1 回のみでは点滅が停止しないこと
- 成功が 2 回連続でも点滅が停止しないこと
- 成功が 3 回連続した時点で点滅が停止すること
- 途中で失敗を挟んだ場合、連続成功の計数がやり直しになること
```

SWR_040 は閾値未満での停止を禁じる要求であるため、閾値ちょうどの回数だけでなく閾値未満の回数を個別に確認する必要がある。
3 回連続の成功だけを確認した場合、1 回の成功で停止する実装も合格してしまう。

## SYS_007（起動と実行基盤の確立）からの派生要件の検証

```{swverif} 第2段ブートローダのチェックサム検証通過の確認
:id: SWV_040
:status: draft
:method: inspection
:environment: ci
:verifies: SWR_041

**前提条件**

- SWR_041 を実装したビルド環境が利用できること
- `make boot2-checksum` が実行できること

**手順**

1. 第2段ブートローダをビルドし、チェックサムを付加した 256 バイトのバイナリを生成する
2. 生成されたバイナリの先頭 252 バイトに対する CRC-32/MPEG-2 を独立に計算する
3. 計算値とバイナリ末尾 4 バイトを比較する
4. 先頭 252 バイトの任意の 1 バイトを変更したバイナリに対して、同じ比較を行う

**合否基準**

- 生成されたバイナリが 256 バイトちょうどであること
- 末尾 4 バイトが先頭 252 バイトに対する CRC-32/MPEG-2 を独立に計算する
- 1 バイトを変更した場合、比較が一致しないこと
```

```{swverif} XIPモードでのフラッシュ実行の確認
:id: SWV_041
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_041

**前提条件**

- SWR_041 を実装したソフトウェアが RP2040 実機に書き込まれていること
- gdb により実行中のプログラムカウンタとメモリを読み出せること

**手順**

1. 電源投入後、USB ブートローダとして列挙されないことを確認する
2. gdb で接続し、アプリケーションのプログラムカウンタがフラッシュ領域（0x10000000 以降）を指していることを確認する
3. フラッシュ領域の任意のアドレスを gdb で読み出し、ビルド成果物の対応する内容と比較する

**合否基準**

- ブート ROM の検証を通過し、USB ブートローダに落ちないこと
- 命令がフラッシュ領域から直接実行されていること（RAM へ全体が複写されていないこと）
- フラッシュ領域の読み出し内容がビルド成果物と一致すること
```

SWR_041 の「ブート ROM による検証を通過すること」は、実機が起動する事実からも分かるが、
チェックサムが偶然一致している可能性や、1 バイト変化で検証が失敗することは確認できない。
チェックサムの正しさは CI 上の計算で、XIP の成立は実機で、それぞれ確認する。

```{swverif} メモリ配置の確認
:id: SWV_042
:status: draft
:method: inspection
:environment: ci
:verifies: SWR_042

**前提条件**

- SWR_042 を実装したビルド環境が利用できること
- `make verify-linker` が実行できること

**手順**

1. リンク済み ELF のセクションヘッダを取得する
2. `.boot2` と `.vectors` の配置アドレスを確認する
3. 初期値を持つデータ領域について、フラッシュ上の配置アドレス（LMA）と実行時アドレス（VMA）を確認する
4. スタック領域の範囲と、他のセクションの範囲が重ならないことを確認する

**合否基準**

- `.boot2` が 0x10000000 に配置されていること
- `.vectors` が 0x10000100 に配置され、256 バイト境界に整列していること
- 初期値を持つデータ領域の LMA がフラッシュ領域、VMA が SRAM 領域にあること
- スタック領域が SRAM 上にあり、他のいずれのセクションとも範囲が重ならないこと
```

配置の検証は静的に完結するため CI 上で自動実行できる。実機での確認は不要である。

```{swverif} C実行環境の初期化の確認
:id: SWV_043
:status: draft
:method: test
:environment: target
:verifies: SWR_043

**前提条件**

- SWR_043 を実装したソフトウェアが RP2040 実機で動作していること
- gdb により変数のアドレスと値を読み出せること

**手順**

1. 既知の初期値を持つグローバル変数と、初期値を持たないグローバル変数を定義する
2. リセット直後、アプリケーションの先頭にブレークポイントを設定して停止させる
3. 両変数の値を gdb で読み出す
4. 初期値を持つ変数の値を書き換えた後、リセットして再度読み出す

**合否基準**

- 初期値を持つ変数が、ソースコードで指定した初期値と一致すること
- 初期値を持たない変数が 0 であること
- リセット後、書き換えた値が初期値に戻ること
```

```{swverif} 未実装例外の既定ハンドラの確認
:id: SWV_044
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_043

**前提条件**

- SWR_043 を実装したソフトウェアが RP2040 実機で動作していること
- gdb により実行を停止させ、プログラムカウンタを読み出せること

**手順**

1. 未実装の例外を意図的に発生させる（未定義命令の実行、または不正アドレスへのアクセス）
2. gdb で実行を停止させ、プログラムカウンタの位置を確認する
3. 一定時間を空けて再度プログラムカウンタを読み出す

**合否基準**

- 既定のハンドラに到達していること
- プログラムカウンタがハンドラ内に留まり、進行しないこと
- 未定義の動作（意図しないアドレスへの飛び先）に陥っていないこと
```

SWR_043 は変数の初期化と例外ハンドラという性質の異なる 2 つを要求している。
前者はアプリケーション開始時点の状態を読み出せば確認できるが、後者は例外を発生させる必要があるため、検証ケースを分ける。

```{swverif} システムクロック周波数の確認
:id: SWV_045
:status: draft
:method: test
:environment: target
:verifies: SWR_044

**前提条件**

- SWR_044 を実装したソフトウェアが RP2040 実機で動作していること
- クロック出力を GPIO へ分周出力できること、または周波数カウンタで計測できること

**手順**

1. システムクロックを分周して GPIO へ出力する
2. 出力周波数を周波数カウンタまたはロジックアナライザで計測する
3. 電源投入から 10 分後に、同じ計測を再度行う
4. クロック源が水晶発振子であることを、クロック制御レジスタの設定値から確認する

**合否基準**

- 計測値から換算したシステムクロックが 125MHz であること
- 水晶発振子の周波数確度の範囲内に収まっていること
- 10 分後の計測値が初回と有意に変わらないこと
- クロック源としてリングオシレータではなく水晶発振子が選択されていること
```

```{swverif} マイクロ秒単位の時間基準の確認
:id: SWV_046
:status: draft
:method: test
:environment: target
:verifies: SWR_044

**前提条件**

- SWR_044 を実装したソフトウェアが RP2040 実機で動作していること
- 時間基準の値を読み出して UART に出力できること
- GPIO のトグル間隔を外部計測できること

**手順**

1. 時間基準の値を連続で 2 回読み出し、差分を記録する
2. 既知の待機時間（1ms、100ms、1s）を時間基準で計測し、値を記録する
3. 同じ待機区間の前後で GPIO をトグルし、外部計測器で実時間を計測する
4. 時間基準による計測値と外部計測値を比較する

**合否基準**

- 連続読み出しの差分が 1 マイクロ秒単位で表現されていること
- 時間基準による計測値と外部計測値の差が、計測区間の 0.1% 以内であること
- 1ms から 1s までの各区間で、上記の精度が保たれること
```

手順 3 の GPIO による外部計測は、時間基準が自己整合的に誤っている場合を検出するために行う。
時間基準の値だけを見た場合、カウンタの分周設定が誤っていても内部では辻褄が合ってしまう。

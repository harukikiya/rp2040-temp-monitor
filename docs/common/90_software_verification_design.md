# ソフトウェア検証設計

> **位置づけ**：本ドキュメントは ASPICE [SWE.6（Software Verification）](https://en.wikipedia.org/wiki/Automotive_SPICE) に該当する。
> ソフトウェア要件（`20_software_requirements.md`）の各要件に対し、統合されたソフトウェアがそれを満たすことを確認する検証ケースを定義する。
> 各 SWV は、対応するソフトウェア要件（SWR_NNN）へのトレースを持つ。

本書では、承認済みのソフトウェア要件に対する検証ケースを、要件の派生元システム要件ごとに整理して記述する。
検証の実施は段階4-4 で行う。

## 検証手段の分類

各検証ケースは `method` 属性で検証手段を示す。分類は IADT に従う。

| 値 | 手段 | 内容 |
| --- | --- | --- |
| `inspection` | 検査 | 成果物を目視または静的解析で確認する。実行を伴わない |
| `analysis` | 解析 | 計算・モデル・シミュレーションにより間接的に確認する |
| `demonstration` | 実証 | 実行し、期待する動作が観測できることを確認する。定量的な計測を伴わない |
| `test` | 試験 | 実行し、定量的な合否基準に対して計測値を判定する |

`environment` 属性は検証を実施する環境を示す。`host`（開発マシン上）、
`target`（RP2040 実機）、`ci`（GitHub Actions）、`review`（人手による確認）のいずれか。

## 検証ケースの構成

各検証ケースは以下の3項目を持つ。

- **前提条件**：検証を実施できる状態の定義。依存する実装段階と必要な計測手段
- **手順**：実施者が再現できる粒度の操作列
- **合否基準**：観測結果に対する判定条件。可能な限り数値で表現する

1つの検証ケースで複数の要件を確認できる場合は、`verifies` に複数の要件 ID を列挙する。

検証ケースの ID（SWV_NNN）は要件 ID（SWR_NNN）と対応しない。
1つの要件が複数の検証ケースを必要とすることも、1つの検証ケースが複数の要件を確認することもある。
対応関係は `verifies` 属性が正であり、番号の一致は偶然である。

TBD 由来の仮置き値は、検証ケースのタイトルに含めない。値は合否基準に書き、
対応する TBD を `open_params` に記録する。タイトルに含めると、TBD の解決に伴う
値の変更がタイトルの変更を招き、PR やコミットメッセージからの参照が切れる。

前提条件には、対象要件を実装したソフトウェアが動作していることと、観測手段が利用できることを書く。
実装を担当する段階番号や内部コンポーネント名は書かない。
SWE.6 は統合されたソフトウェアが要件を満たすことを確認するプロセスであり、内部構造への言及はコンポーネント検証（SWE.5.2）の領域である。
またアーキテクチャは段階2.5-1 で作り直すため、現時点のコンポーネント名を埋め込むと再設計の結果に連動して修正が必要になる。

本書で初出の略語は、初出箇所で展開形を併記する。

## SYS_001（温度の継続的変化可視化）からの派生要件の検証

```{swverif} ADC初期化と温度センサチャネル選択の確認
:id: SWV_001
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_001

**前提条件**

- SWR_001 を実装したソフトウェアが RP2040 実機で動作していること
- UART ログ出力が利用できること

**手順**

1. ADC 初期化後、ADC の制御レジスタと入力選択レジスタの値を UART に出力する
2. 温度センサのバイアス有効化ビットの状態を UART に出力する
3. ADC 変換を1回実行し、取得した生値を UART に出力する

**合否基準**

- 入力チャネルとして ADC チャネル4 が選択されていること
- 温度センサのバイアスが有効であること
- 取得した生値が 0 でも 0xFFF でもないこと（室温付近で飽和していないこと）
```

```{swverif} DMA転送の継続性とラップアラウンドの確認
:id: SWV_002
:status: draft
:method: test
:environment: target
:verifies: SWR_002

**前提条件**

- SWR_002 と SWR_007 を実装したソフトウェアが RP2040 実機で動作していること
- リングバッファの内容と DMA の書き込み位置を UART で観測できること

**手順**

1. リングバッファ全体と DMA の現在の書き込み位置を 1 秒間隔で UART に出力する
2. リングバッファが 3 周する時間以上（バッファ長 × サンプリング周期 × 3 以上）動作させる
3. 各出力における書き込み位置と、バッファ内の値の更新状況を記録する

**合否基準**

- 書き込み位置がバッファ長で折り返し、途切れずに進み続けること
- ラップアラウンド後もバッファの全要素が更新され続けること
- 転送されたデータが 16bit 単位で欠落なく並んでいること（ADC の変換結果として妥当な範囲に収まり、バイト境界のズレがないこと）
```

```{swverif} DMA転送のCPU非介入の確認
:id: SWV_005
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_002

**前提条件**

- SWR_002 を実装したソフトウェアが RP2040 実機で動作していること
- gdb により CPU を停止させた状態で DMA の転送カウンタを読み出せること

**手順**

1. サンプリングを開始し、DMA が転送している状態を確認する
2. gdb でブレークポイントを設定し、CPU を停止させる
3. CPU 停止中に DMA の転送カウンタを 2 回、一定期間を空けて読み出す

**合否基準**

- CPU 停止中も DMA の転送カウンタが進んでいること
- CPU 停止中も ADC FIFO のオーバーフローフラグが立たないこと
```

SWR_002 は「DMA 転送を行うこと」と「CPU 介入なしに継続すること」の 2 つを要求している。
前者は動作を計測すれば確認できるが、後者は動作している事実からは証明できないため、
CPU を停止させた状態で転送が継続することを示す別の検証ケースを設ける。

```{swverif} サンプリング周期の計測
:id: SWV_003
:status: draft
:method: test
:environment: target
:verifies: SWR_003
:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_003 を実装したソフトウェアが RP2040 実機で動作していること
- GPIO のトグル出力をロジックアナライザまたはオシロスコープで観測できること

**手順**

1. ADC サンプリング開始時に GPIO をトグルする計測用コードを有効化する
2. 10 秒間動作させ、トグル間隔を記録する

**合否基準**

- トグル間隔の平均が 10ms ± 0.1ms の範囲内であること
- 連続する 1000 回の計測で 10ms ± 0.5ms を外れる回数が 0 であること
```

```{swverif} I2Cバス初期化と通信確立の確認
:id: SWV_004
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_004
:open_params: TBD-010 I2C速度が未確定のため、合否基準の通信速度は仮置き。解決時期: 段階3-2。TBD-017 GPIO割り当てが未定のため、観測対象のピンは未確定。解決時期: 段階3-2。

**前提条件**

- SWR_004 を実装したソフトウェアが RP2040 実機で動作していること
- I2C の SCL（Serial Clock Line、シリアルクロック線）と SDA（Serial Data Line、シリアルデータ線）をロジックアナライザまたはオシロスコープで観測できること

**手順**

1. I2C 初期化後、LCD1602 のアドレスに対してダミー書き込みを1回行う
2. SCL のクロック周波数を計測する
3. 9 クロック目における ACK（Acknowledge、受信確認応答）の有無を観測する

**合否基準**

- SCL の周波数が 100kHz ± 10% の範囲内であること
- スレーブから ACK が返ること（NACK ではないこと）
- バスがアイドル時に SCL・SDA ともに H レベルに保持されていること（プルアップが効いていること）
```

```{swverif} LCD1602の初期化シーケンスの確認
:id: SWV_006
:status: draft
:method: inspection
:environment: review
:verifies: SWR_005

**前提条件**

- SWR_005 を実装したソースコードが存在すること
- HD44780 のデータシートに記載された初期化シーケンスを参照できること

**手順**

1. LCD の初期化処理のソースコードを読み、送信するコマンド列とその順序を書き出す
2. HD44780 のデータシートが規定する 4 ビットモードの初期化シーケンスと突き合わせる
3. 各コマンド間のディレイの実装値を、データシートが要求する最小値と比較する

**合否基準**

- コマンド列と順序がデータシートの初期化シーケンスと一致すること
- 各ディレイがデータシートの要求する最小値以上であること
- PCF8574（I2C 経由の 8 ビット I/O エクスパンダ）への書き込みが上位 4 ビットと下位 4 ビットに分割されていること
```

```{swverif} LCD1602への表示成立の確認
:id: SWV_007
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_005

**前提条件**

- SWR_004 と SWR_005 を実装したソフトウェアが RP2040 実機で動作していること
- LCD1602 が接続され、目視できること

**手順**

1. 初期化後、既知の文字列を 1 行目と 2 行目に書き込む
2. 表示内容を目視で確認する

**合否基準**

- 意図した文字列が意図した行・桁位置に表示されること
- 文字化け、欠落、ゴースト表示がないこと
```

SWR_005 の初期化シーケンスの妥当性は、表示ができることでは保証されない。
ディレイが不足していても室温では動作してしまい、個体差や温度条件で失敗する不具合になるため、
データシートと突き合わせを検査として分離する。一方で配線やアドレス指定の誤りは検査では見つからないため、実機での表示検証も行う。

```{swverif} 表示更新周期の計測
:id: SWV_008
:status: draft
:method: test
:environment: target
:verifies: SWR_006
:open_params: TBD-001 表示周期500msは仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_006 を実装したソフトウェアが RP2040 実機で動作していること
- LCD への書き込み開始時点を GPIO のトグルまたは I2C バスの通信開始で観測できること

**手順**

1. LCD 表示更新の開始時点を観測できる状態にする
2. 60 秒間動作させ、更新間隔を全て記録する

**合否基準**

- 更新間隔の平均が 500ms ± 10ms の範囲内であること
- 記録した全区間において、更新間隔が 500ms を下回らないこと
```

SWR_006 は「500ms 周期で更新する」ことに加えて「500ms 未満の周期で更新しない」ことを要求している。
平均値だけでは後者を確認できないため、全区間の最小を合否基準に含める。

## SYS_002（統計値の算出と表示）からの派生要件の検証

```{swverif} リングバッファのサイズと要素型の確認
:id: SWV_009
:status: draft
:method: analysis
:environment: review
:verifies: SWR_007
:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、必要サンプル数が連動して変わる。解決時期: 段階4-4。:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、必要サンプル数が連動して変わる。解決時期: 段階4-4。TBD-004 トレンド判定対象期間1分は仮置きのため、必要サンプル数が連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_007 を実装したソースコードが存在すること
- SYS_003 が規定するトレンド判定対象期間が確定していること

**手順**

1. トレンド判定対象期間とサンプリング周期から、保持すべき最小サンプル数を算出する
2. 算出値以上となる最小の 2 の冪乗を求める
3. 実装されたバッファサイズおよび要素型の定義と突き合わせる

**合否基準**

- バッファサイズが 2 の冪乗であること
- バッファサイズが手順 2 で求めた値と一致すること
- 要素が 16bit unsigned integer であること
```

```{swverif} リングバッファのハードウェア非依存性の確認
:id: SWV_010
:status: draft
:method: demonstration
:environment: host
:verifies: SWR_007

**前提条件**

- SWR_007 を実装したソースコードが存在すること
- 開発マシン上の C コンパイラが利用できること

**手順**

1. リングバッファのソースファイルを、RP2040 向けではなく開発マシン向けにビルドする
2. ビルドしたオブジェクトをホスト上で実行し、要素の書き込みと読み出しを行う

**合否基準**

- RP2040 固有のヘッダやレジスタ定義を参照せずビルドが完了すること
- ホスト上で書き込みと読み出しが期待通り動作すること
```

SWR_007 は「データ構造の仕様」と「ハードウェア非依存であること」を要求している。
前者は実装を読めば確認できるが、後者はホスト環境でビルドおよび実行が成立することを示す方が確実である。
レジスタアクセスの有無をコードの目視だけで保証するのは、間接的な include を見落とす危険がある。

```{swverif} スナップショット取得の順序と範囲の確認
:id: SWV_011
:status: draft
:method: test
:environment: host
:verifies: SWR_008

**前提条件**

- SWR_007 と SWR_008 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. リングバッファに既知の連番データを書き込む
2. 書き込み位置がバッファの途中にある状態で、指定サンプル数のスナップショットを取得する
3. 書き込み位置がラップアラウンドを跨いだ状態で、同様にスナップショットを取得する
4. 指定サンプル数をバッファ長と等しくした場合、および 1 とした場合をそれぞれ取得する

**合否基準**

- 取得したサンプル列が、書き込み位置から遡って古い順に並んでいること
- ラップアラウンドを跨ぐ場合も、途切れや重複なく連続していること
- 指定サンプル数と取得件数が一致すること
```

SWR_008 が許容する「最大1サンプル分の不整合」は DMA による並行書き込みが前提であり、ホスト環境では再現しない。
本検証はスナップショットの順序と範囲の正しさに限定し、並行書き込み下での挙動は実機での確認に委ねる。

```{swverif} 統計値計算の正しさの確認
:id: SWV_012
:status: draft
:method: test
:environment: host
:verifies: SWR_009

**前提条件**

- SWR_009 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 最大値・最小値・平均値が既知であるサンプル列を入力し、計算結果を取得する
2. 全要素が同一値のサンプル列を入力する
3. 16bit unsigned integer の最大値（65535）を含むサンプル列を入力する
4. 入力サンプル数を変えて、それぞれ計算結果を取得する

**合否基準**

- 最大値・最小値が入力の実際の最大・最小と一致すること
- 平均値が期待値と一致すること（丸め方は実装の定義に従い、期待値もそれに合わせる）
- 全要素同一の場合、最大値・最小値・平均値が全てその値と一致すること
- 指定したサンプル数が計算に反映されること
- 総和が 16bit の範囲を超える入力でも平均値が正しく求まること
```

```{swverif} 「最新値」の算出範囲の確認
:id: SWV_013
:status: draft
:method: test
:environment: host
:verifies: SWR_010
:open_params: TBD-002 サンプリング周期100Hzは仮置きのため、直近1秒に相当するサンプル数が連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_009 と SWR_010 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 直近 1 秒分とそれ以前で異なる値を持つサンプル列をリングバッファに書き込む
2. 「最新値」を取得する

**合否基準**

- 直近 1 秒分に相当するサンプル数のみが平均の対象になっていること
- 1 秒より前のサンプルが結果に影響しないこと
```

```{swverif} 温度変換の正しさと分解能の確認
:id: SWV_014
:status: draft
:method: test
:environment: host
:verifies: SWR_011

**前提条件**

- SWR_011 を実装したソフトウェアがホスト上でビルドできること
- RP2040 のデータシートに記載された温度変換式を参照できること

**手順**

1. データシートの変換式から、代表的な ADC 生値に対する期待温度を手計算で求める
2. 同じ ADC 生値を入力し、変換結果と期待値を比較する
3. 隣接する ADC 生値（N と N+1）を入力し、出力の差を確認する
4. 較正係数を変更し、同じ ADC 生値に対する出力の変化を確認する

**合否基準**

- 変換結果がデータシートの式から求めた期待値と一致すること（固定小数点実装の場合、丸め誤差は分解能の範囲内に収まること）
- 出力の分解能が 0.1℃ 以上であること
- 較正係数の変更が出力に反映されること
```

内部表現（浮動小数点か固定小数点か）は SWR_011 が規定しておらず段階2.5-3 で決定する。
本検証は入出力の関係のみを対象とし、内部表現には依存しない形で合否基準を定める。

```{swverif} 表示フォーマットの桁数と内容の確認
:id: SWV_015
:status: draft
:method: test
:environment: host
:verifies: SWR_012
:open_params: TBD-011 表示レイアウトの最終形が未確定のため、配置に関する合否基準は暫定。解決時期: 段階4-4。

**前提条件**

- SWR_012 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 統計値（最新値・最大値・最小値・平均値）に代表的な値を与え、生成される文字列を取得する
2. 温度が負値の場合、および 3 桁になる場合の文字列を取得する
3. 各行の文字数を数える

**合否基準**

- 各行が 16 文字以内であること
- 行数が 2 行以内であること
- 最新値・最大値・最小値・平均値の 4 値が全て含まれること
- 温度が小数点以下 1 桁まで表示されること
- 負値および 3 桁の値でも桁あふれしないこと
```

## SYS_003（温度変化のトレンド表示）からの派生要件の検証

```{swverif} トレンド判定モジュールの提供機能の確認
:id: SWV_016
:status: draft
:method: test
:environment: host
:verifies: SWR_013

**前提条件**

- SWR_013 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 上昇・下降・横ばいのそれぞれに該当するサンプル列を用意する
2. 各サンプル列を入力し、判定結果を取得する

**合否基準**

- 3 つの傾向がいずれも判定結果として得られること
- 同一の入力に対して常に同一の結果が返ること
```

```{swverif} トレンド判定のモジュールのハードウェア被依存性の確認
:id: SWV_017
:status: draft
:method: demonstration
:environment: host
:verifies: SWR_013

**前提条件**

- SWR_013 を実装したソースコードが存在すること
- 開発マシン上の C コンパイラが利用できること

**手順**

1. トレンド判定モジュールを、統計モジュールともに開発マシン向けにビルドする
2. ビルドしたオブジェクトをホスト上で実行し、判定を行う

**合否基準**

- RP2040 固有のヘッダやレジスタ定義を参照せずにビルドが完了すること
- ホスト上で判定が期待通り動作すること
- 統計モジュール以外のモジュールへの依存が生じていないこと
```

```{swverif} 前半後半平均比較アルゴリズムの確認
:id: SWV_018
:status: draft
:method: test
:environment: host
:verifies: SWR_014
:open_params: TBD-004 トレンド判定対象期間1分は仮置きのため、分割されるサンプル数が連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_009 と SWR_014 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 前半と後半で平均が既知の差を持つサンプル列を用意し、判定に用いられた差分値を取得する
2. 判定対象期間と同数のサンプルを与え、前半・後半それぞれの平均を手計算の値と比較する
3. 判定対象期間の中央付近にのみ大きな変化があるサンプル列を与え、差分値を確認する

**合否基準**

- 対象期間が前半と後半に二等分されていること
- 前半・後半の平均が手計算の値と一致すること
- 差分が「後半平均 - 前半平均」の順で計算されていること（温度が上昇するサンプル列に対して差分が正になること）
- 中央付近の一時的な変化が、前半・後半の平均に希釈されて差分に強く現れないこと
```

手順 3 は、始点と終点の単純比較ではなく平均の比較を採用した理由
（SWR_014 の rationale が挙げるノイズへのロバスト性）が実際に成立していることの確認である。
実装が単純比較になっていた場合、手順 1・2 だけでは差が現れないことがある。

```{swverif} トレンド判定の閾値と境界の確認
:id: SWV_019
:status: draft
:method: test
:environment: host
:verifies: SWR_015
:open_params: TBD-012 閾値0.3℃は仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_014 と SWR_015 を実装したソフトウェアがホスト上でビルドできること

**手順**

1. 差分が閾値をわずかに超える正の値となるサンプル列を入力する
2. 差分が閾値とちょうど等しい正の値となるサンプル列を入力する
3. 差分が閾値をわずかに下回る正の値となるサンプル列を入力する
4. 手順 1〜3 と同じ大きさの負の差分となるサンプル列をそれぞれ入力する

**合否基準**

- 差分が閾値を超え正の値のとき、上昇傾向と判定されること
- 差分が閾値を超え負の値のとき、下降傾向と判定されること
- 差分の絶対値が閾値と等しいとき、横ばいと判定されること
- 差分の絶対値が閾値未達のとき、横ばいと判定されること
```

SWR_015 は「0.3℃を超え」「0.3℃以下であれば横ばい」と境界の扱いを明記している。
境界値そのものを与える手順 2 を含めない限り、不等号の向きの誤り（`>` と `>=` の取り違え）は検出できない。

```{swverif} トレンド判定の実行周期と表示との同期の確認
:id: SWV_020
:status: draft
:method: test
:environment: target
:verifies: SWR_016
:open_params: TBD-001 実行周期500msは仮置きのため、合否基準の値は連動して変わる。解決時期: 段階4-4。

**前提条件**

- SWR_016 を実装したソフトウェアが RP2040 実機で動作していること
- トレンド判定の開始時点と LCD 表示更新の開始時点を、それぞれ GPIO のトグルで観測できること

**手順**

1. トレンド判定の開始時点と表示更新の開始時点を、別々の GPIO で観測できる状態にする
2. 60 秒間動作させ、両者の発生時刻を全て記録する

**合否基準**

- トレンド判定の実行間隔の平均が 500ms ± 10ms の範囲内であること
- トレンド判定の回数と表示更新の回数が一致すること
- 各表示更新の直前にトレンド判定が完了していること
```

SWR_016 は実行周期に加えて「結果を表示に反映すること」を要求している。
周期の計測だけでは、判定結果が表示に届く前に上書きされる実装を検出できないため、両者の順序と回数の一致を合否基準に含める。

```{swverif} トレンド記号の表示の確認
:id: SWV_021
:status: draft
:method: demonstration
:environment: target
:verifies: SWR_017
:open_params: TBD-013 表示記号の最終形が未確定のため、合否基準の記号は暫定。解決時期: 4-4。

**前提条件**

- SWR_012 と SWR_017 を実装したソフトウェアが RP2040 実機で動作していること
- LCD1602 が接続され、目視できること
- 温度を意図的に上昇・下降させる手段があること（センサ部への加温・冷却）

**手順**

1. 温度を上昇させ、判定が上昇傾向に変わった時点の表示を確認する
2. 温度を下降させ、判定が下降傾向に変わった時点の表示を確認する
3. 温度を安定させ、横ばいと判定される状態の表示を確認する

**合否基準**

- 上昇・下降・横ばいの 3 状態が、それぞれ異なる記号で表示されること
- 記号が SWR_012 の表示フォーマット中に収まり、他の表示項目を欠落させないこと
- 記号が判定結果と一致すること
```

記号の具体的な字形は「TBD-013」により未確定であるため、合否基準では「3. 状態が区別できること」と「フォーマットに収まること」の限定する。
字形の決定は段階4-4 で実機の視認性を確認した後に行う。

